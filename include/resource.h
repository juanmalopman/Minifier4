#pragma once

//
// DEFINES
//


#define CREATEPROCESS_MANIFEST_RESOURCE_ID_customDefine 1 // Avoid linking the whole windows.h in resource.rc. The Resource Compiler (rc.exe) fails to parse it.
#define RT_MANIFEST_customDefine 24  // Avoid linking the whole windows.h in resource.rc. The Resource Compiler (rc.exe) fails to parse it.
#define APP_ICON 100

